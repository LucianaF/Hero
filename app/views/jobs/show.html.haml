
		%h1= @job.Titulli
		%p= @job.Pershkrimi
		%p= @job.kompania
		
		= link_to "Home", root_path
		= link_to "Edit", edit_job_path(@job)
		= link_to "Delete", job_path(@job)method: :delete, data: { confirm: "Jeni i sigurte?" }